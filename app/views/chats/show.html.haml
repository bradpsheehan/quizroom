.row
  .large-11.columns
    %ul#chat
    = form_tag classroom_chat_path(params[:classroom_id], params[:id]), method: :put, remote: true do
      .field
        = label_tag :message
        = text_area_tag :message
        = submit_tag "post", {id: "chat-submit"}


= render 'web_socket.js'
